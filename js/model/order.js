class Order {
   constructor(order_status, order_amount, client, products_order){
    order_status     = order_status;
    order_amount     = order_amount;
    client           = client;
    products_order   = products_order;
}
}